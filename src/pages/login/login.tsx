import { Centered } from '../../components/centered'
import { Login } from '../../components/login'

export const LoginPage = () => {

    return (
        <Centered>
            <Login />
        </Centered>
    )
}