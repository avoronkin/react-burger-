import { Centered } from '../../components/centered'
import { Logout } from '../../components/logout'

export const LogoutPage = () => {

    return (
        <Centered>
            <Logout />
        </Centered>
    )
}