import { ProfileForm } from './profile-form'
import { ProfileLayout } from './profile-layout'

export const Profile = () => {
    return (
        <ProfileLayout>
            <ProfileForm />
        </ProfileLayout>
    )
}
